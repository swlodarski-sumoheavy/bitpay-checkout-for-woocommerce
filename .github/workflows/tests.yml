name: Tests

on: [push, pull_request]

jobs:
  unit:
    runs-on: ubuntu-20.04

    strategy:
      fail-fast: false
      matrix:
        php-version: [8.1, 8.2, 8.3]

    services:
      mysql:
        image: mysql:latest
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: false
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: bitpay-wordpress-test
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - uses: actions/checkout@v4
      - name: Install Composer dependencies
        run: composer install --no-progress --prefer-dist --optimize-autoloader
      - name: Install WordPress with WooCommerce
        run: |
          cd ..
          rm -fr wordpress
          mkdir wordpress
          cd wordpress
          curl -O https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
          php wp-cli.phar core download
          php wp-cli.phar config create --dbname=bitpay-wordpress-test --dbuser=root --dbpass=root --dbhost=127.0.0.1
          php wp-cli.phar core install --url=bitpay-wordpress.test --title=Example --admin_user=admin --admin_password=admin --admin_email=admin@example.com
          php wp-cli.phar plugin install woocommerce
          cp -R ../bitpay-checkout-for-woocommerce wp-content/plugins/bitpay-checkout-for-woocommerce
          cp ../bitpay-checkout-for-woocommerce/tests/Unit/wp-config.tests.php wp-content/plugins/bitpay-checkout-for-woocommerce/tests/Unit/wp-config.php
          cp ../bitpay-checkout-for-woocommerce/tests/Unit/index.tests.php wp-content/plugins/bitpay-checkout-for-woocommerce/index.php
      - name: Run tests
        run: |
          cd ../wordpress/wp-content/plugins/bitpay-checkout-for-woocommerce
          ./vendor/bin/phpunit -c phpunit.xml.dist

  # e2e:
  #   runs-on: ubuntu-20.04

  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       php-version: [8.1]
  #       # php-version: [8.1, 8.2, 8.3]

  #   services:
  #     mysql:
  #       image: mysql:latest
  #       env:
  #         MYSQL_ALLOW_EMPTY_PASSWORD: false
  #         MYSQL_ROOT_PASSWORD: root
  #         MYSQL_DATABASE: bitpay-wordpress-test
  #       ports:
  #         - 3306:3306
  #       options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
  #     selenium:
  #       image: selenium/standalone-chrome:3.141.5
  #       ports:
  #         - 4444:4444

  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Setup PHP
  #       uses: shivammathur/setup-php@v2
  #       with:
  #         php-version: ${{ matrix.php-version }}
  #         tools: phpunit-bridge
  #         extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, mysql
  #         coverage: xdebug
  #     - name: Prepare environment
  #       run: |
  #         chmod 755 .github/workflows/acceptance/acceptance-tests.sh
  #         ./.github/workflows/acceptance/acceptance-tests.sh
  #         sudo apt install libapache2-mod-php${{ matrix.php-version }}
  #         sudo a2enmod php${{ matrix.php-version }} rewrite
  #         sudo systemctl restart apache2
  #     - name: Install Composer dependencies
  #       run: composer install --no-progress --prefer-dist --optimize-autoloader
  #     - name: Install WordPress with WooCommerce
  #       run: |
  #         cd ..
  #         rm -fr wordpress
  #         mkdir wordpress
  #         cd wordpress
  #         curl -O https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
  #         php wp-cli.phar core download
  #         php wp-cli.phar config create --dbname=bitpay-wordpress-test --dbuser=root --dbpass=root --dbhost=127.0.0.1
  #         # php wp-cli.phar core install --url=bitpay-wordpress.test --title=Example --admin_user=admin --admin_password=admin --admin_email=admin@example.com
  #         php wp-cli.phar plugin install woocommerce
  #         cp -R ../bitpay-checkout-for-woocommerce wp-content/plugins/bitpay-checkout-for-woocommerce
  #         cp ../bitpay-checkout-for-woocommerce/tests/.env.dist wp-content/plugins/bitpay-checkout-for-woocommerce/tests/.env
  #         # php wp-cli.phar plugin activate bitpay-checkout-for-woocommerce
  #         # php wp-cli.phar db export --add-drop-table db.sql
  #         cd ..
  #         sudo rm -fr /var/www/html/index.*
  #         sudo mv wordpress/* /var/www/html
  #     - name: Run tests
  #       run: |
  #         wget http://bitpay-wordpress.test/wp-login.php -O tt
  #         cat tt
  #         cd /var/www/html/wp-content/plugins/bitpay-checkout-for-woocommerce
  #         ./vendor/bin/codecept run --debug
  #     - name: Upload artifacts
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: artifacts
  #         path: /var/www/html/wp-content/plugins/bitpay-checkout-for-woocommerce/tests/EndToEnd/_output/

  # e2e:
  #   runs-on: ubuntu-latest
  #   container:
  #     image: php:8.1-apache
  #     ports:
  #       - 80:80
  #     options: --network-alias=bitpay-wordpress.test

  #   strategy:
  #     fail-fast: false

  #   env:
  #     TEST_BASE_URL: http://bitpay-wordpress.test
  #     SELENIUM_HOST: http://selenium:4444/wd/hub

  #   services:
  #     mysql:
  #       image: mysql:latest
  #       env:
  #         MYSQL_ALLOW_EMPTY_PASSWORD: false
  #         MYSQL_ROOT_PASSWORD: root
  #         MYSQL_DATABASE: bitpay-wordpress-test
  #       ports:
  #         - 3306:3306
  #       options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
  #     selenium:
  #       image: selenium/standalone-chrome
  #       ports:
  #         - 4444:4444

  #   steps:
  #     - uses: actions/checkout@v4
  #     # - name: Prepare environment
  #     #   run: |
  #     #     chmod 755 .github/workflows/acceptance/acceptance-tests.sh
  #     #     ./.github/workflows/acceptance/acceptance-tests.sh
  #     #     sudo apt install libapache2-mod-php${{ matrix.php-version }}
  #     #     sudo a2enmod php${{ matrix.php-version }} rewrite
  #     #     sudo systemctl restart apache2
  #     - name: Install Composer dependencies
  #       run: |
  #         curl -sS https://getcomposer.org/installer -o composer-setup.php
  #         php composer-setup.php --install-dir=/usr/local/bin --filename=composer
  #         composer install --no-progress --prefer-dist --optimize-autoloader
  #     - name: Install WordPress with WooCommerce
  #       run: |
  #         cd ..
  #         rm -fr wordpress
  #         mkdir wordpress
  #         cd wordpress
  #         curl -O https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
  #         php wp-cli.phar core download
  #         php wp-cli.phar config create --dbname=bitpay-wordpress-test --dbuser=root --dbpass=root --dbhost=127.0.0.1
  #         mysql -h127.0.0.1 -uroot -proot bitpay-wordpress-test <./.github/workflows/acceptance/dump.sql && echo "Restored Database"
  #         cp -R ../bitpay-checkout-for-woocommerce wp-content/plugins/bitpay-checkout-for-woocommerce
  #         cp ../bitpay-checkout-for-woocommerce/tests/.env.dist wp-content/plugins/bitpay-checkout-for-woocommerce/tests/.env
  #         cd ..
  #         sudo rm -fr /var/www/html/index.*
  #         sudo mv wordpress/* /var/www/html
  #     - name: Restart apache server
  #       run: service apache2 restart
  #     - name: Check if site is accessible
  #       run: |
  #         curl $TEST_BASE_URL
  #     - name: Run tests
  #       run: |
  #         cd /var/www/html/wp-content/plugins/bitpay-checkout-for-woocommerce
  #         ./vendor/bin/codecept run --debug
